# 뉴스레터 프로젝트 - 분석 및 문제 해결 기록

작성일: 2026-08-25
대상: `backend/` (RSS + RAG 뉴스레터 파이프라인)

---

## 1. 하려던 것

```
뉴스 RSS 수집 → JSON → RAG → 실제 응답 확인 → GitHub push
```

확인 목표 두 가지: **리서치가 되는가**, **요약을 잘 해주는가**

---

## 2. 발견한 문제와 해결

### 문제 1. 네이버 RSS가 죽어 있었다

**증상**
```
naver_tech  0건  → <urlopen error [Errno 11001] getaddrinfo failed>
```

**원인**
`rss.naver.com` 도메인 자체가 존재하지 않는다. 네이버가 RSS 서비스를 종료했다.
`getaddrinfo failed`는 "주소를 못 찾겠다"는 뜻, 즉 서버가 죽은 게 아니라 **주소가 없다**는 것.

**해결**
피드 목록에서 제거하고, 국내 언론사 25곳을 실제로 호출해 살아있는 것만 남겼다.

| 결과 | 매체 |
|---|---|
| 정상 (16곳) | 연합뉴스, 연합뉴스_경제, 한겨레, 경향신문, 동아일보, 조선일보, 서울신문, 매일경제, 매일경제_경제, 머니투데이, 뉴시스, 전자신문, 전자신문_IT, ZDNet코리아, 노컷뉴스, 오마이뉴스 |
| 실패 | 중앙일보, 한국경제, 이데일리, KBS, SBS, MBC, YTN, 연합뉴스_IT |

---

### 문제 2. 구글뉴스는 링크가 상세페이지로 안 간다

**증상**
```
link: https://news.google.com/rss/articles/CBMiWkFVX3lxTE92Vm1KZjI5...
```

**원인**
구글뉴스 RSS는 원문 주소 대신 **자기네 리다이렉트 주소**를 준다.
따라가 봐도 자바스크립트로 넘기는 구조라 최종 URL이 안 나온다.
`description`도 본문이 아니라 관련기사 링크 HTML 덩어리였다.

**영향**
- 사용자가 기사 제목을 눌러도 원문으로 못 감
- 본문 크롤링 불가

**해결**
구글뉴스를 아예 제외했다. 대신 원문 링크를 주는 국내 언론사 16곳으로 커버.

---

### 문제 3. RSS는 본문 전체를 주지 않는다

**측정 결과**

| 매체 | RSS가 주는 길이 |
|---|---|
| 연합뉴스 | 63자 |
| 한겨레 | 464자 |
| ZDNet | 1,233자 |

평균 458자. 요약 재료로는 부족하다.

**해결**
`article_fetcher.py`를 만들어 링크를 따라가 본문을 추출했다.
- `requests`로 페이지를 받고 `BeautifulSoup`으로 본문 영역 선택
- 언론사마다 본문 태그가 달라서 후보 선택자 12개를 순서대로 시도
- "무단전재 금지", "기자 =" 같은 잡음 문구 제거

**결과**

| 항목 | 개선 전 | 개선 후 |
|---|---|---|
| 기사당 텍스트 | 458자 | **900자** |
| 성공률 | — | **155/156 (99%)** |

조선일보·머니투데이 일부는 봇 차단으로 실패 → RSS 요약으로 자동 대체.

---

### 문제 4. RAG가 근거를 찾아놓고 "없다"고 답했다

**증상**

"AI·반도체 관련 소식 알려줘" 라고 물었더니:
```
[답변] 수집된 기사에는 없습니다.
[근거] [2] 美증시 반도체 급락…마이크론 -5.8%
       [3] 엔비디아 호실적이면 반도체주 다시 뛸까
```
근거로 반도체 기사를 가져와 놓고 없다고 답하는 모순.

**원인**
`_format_context()`가 LLM에게 넘길 때 **제목을 빼고** 있었다.

```python
f"{d.page_content.splitlines()[-1][:300]}"   # 마지막 줄 = '내용' 만
```
구글뉴스 기사는 '내용'이 HTML 덩어리라, 모델 입장에서는 의미 없는 문자열만 받은 셈.
제목에 "엔비디아", "반도체"가 있어도 모델은 볼 수 없었다.

**해결**
- 제목과 내용을 함께 넘기도록 수정 (`page_content` 전체 사용)
- 프롬프트에 "제목만으로 알 수 있는 사실도 답한다" 규칙 추가

---

### 문제 5. 부고·인사 기사가 뉴스레터에 들어갔다

**증상**

"오늘의 주요 뉴스" 요약 결과:
```
- 덕성여대 인사 발표
- 부고 소식 - 이성한 선임기자 부친상
```

**원인**
검색어 "오늘의 주요 뉴스"와 기사 제목 "오늘의 인사", "오늘의 부고"가
글자상 비슷해서 벡터 검색 상위에 올라왔다.

**해결**
수집 단계에서 정형 기사를 걸러내는 `SKIP_TITLE` 정규식 추가.
부고·인사·헤드라인 목록·다시보기·포토뉴스·카드뉴스 제외.

**결과** 재실행 시 해당 기사 0건, 요약이 실제 뉴스로 채워짐.

---

### 문제 6. `main.py` 가 없는 파일을 불러왔다

**증상**
```
ModuleNotFoundError: No module named 'newsletter_agent'
```

**원인**
이전에 `newsletter_agent.py`를 `project_newsletter.py`로 이름을 바꿨는데,
`main.py`의 import 문은 그대로 옛 이름을 가리키고 있었다.

**해결**
```python
from project_newsletter import NewsletterAgent
```

---

### 문제 7. LangChain 문법이 바뀌어 서버가 안 켜졌다  ← 로컬 실행 실패의 원인

**증상**

`127.0.0.1:8000/docs` 접속 시 `ERR_CONNECTION_REFUSED`.
서버가 뜨자마자 종료되고 있었다.

```
File "project_newsletter.py", line 3, in <module>
    from langchain.agents import AgentExecutor, create_openai_tools_agent
ImportError
```

**원인**

`requirements.txt`는 `langchain==0.1.0`으로 적혀 있었지만 실제 설치된 건 **1.3.17**.
LangChain이 0.x → 1.x로 올라가며 `langchain.agents`의 `create_openai_tools_agent`가
사라졌다. **버전이 올라가면서 옛 함수가 없어진 경우**다.

**해결**

그 import는 파일 안에서 **한 번도 쓰이지 않는 코드**였다. 지우면 끝.

```python
# 삭제
from langchain.agents import AgentExecutor, create_openai_tools_agent
```

`requirements.txt`도 실제 설치 버전에 맞게 갱신했다.

---

### 문제 8. 라우터가 등록 안 된 줄 알았던 착각

**증상**

라우트 목록을 찍어보니 `/rag/*`가 안 보였다.

**실제**

최신 FastAPI는 `include_router()`로 넣은 라우터를 낱개로 펼치지 않고
`_IncludedRouter` 객체 하나로 묶어 보관한다. 확인 방법이 틀렸을 뿐,
등록은 정상이었다. 서버를 띄워 실제 호출해보니 200 OK.

> 교훈: 코드 내부를 들여다보는 것보다 **실제로 호출해보는 것**이 정확하다.

---

## 3. 최종 검증 결과

```
1. RSS 수집          152건 / 16개 매체
2. 본문 확보          155/156 성공 (99%), 458자 → 900자
3. 색인 생성          156개 문서 (FAISS)
4. 리서치             근거 기사 번호 + 원문 링크와 함께 정상 답변
5. 요약 3종           brief / newsletter / deep 모두 정상
6. 환각 방지          "화성 이주 예산" → "수집된 기사에는 없습니다"
   전체 소요          78초
```

엔드포인트 응답 확인:

| 경로 | 결과 |
|---|---|
| `GET /health` | 200 |
| `GET /rag/styles` | 200 |
| `GET /rag/status` | 200 |

---

## 4. 요약 3종

| style | 형식 | 참고 기사 |
|---|---|---|
| `brief` | 한 줄 + 핵심 3가지, 200자 이내 | 5건 |
| `newsletter` | 이슈 3~5개 (소제목 + 설명) + 오늘의 한 줄 | 8건 |
| `deep` | 무슨 일이 / 왜 중요한가 / 함께 볼 흐름 / 참고 기사 | 12건 |

---

## 5. 엔드포인트 설계 이유

수집은 1~2분 걸리고 요약은 몇 초다. 이걸 한 API에 넣으면 매번 2분을 기다려야 한다.
그래서 **준비 단계와 사용 단계를 나눴다.**

```
POST /rag/build       하루 1~2회. 수집 → 본문 → 색인      (느림)
      ↓ 색인이 디스크에 저장됨
POST /rag/ask         질문할 때마다                        (빠름)
POST /rag/summarize   요약할 때마다                        (빠름)
```

| 메서드 | 경로 | 설명 |
|---|---|---|
| POST | `/rag/build` | 수집 + 본문 크롤링 + 색인 |
| GET | `/rag/status` | 색인 준비 상태 |
| GET | `/rag/news` | 원본 뉴스 JSON (링크 포함) |
| POST | `/rag/ask` | 리서치 |
| POST | `/rag/summarize` | 요약 (style 선택) |
| POST | `/rag/summarize/compare` | 3종 비교 |
| GET | `/rag/styles` | 스타일 목록 |

---

## 6. 실행 방법

```bash
cd C:\python_workspace\노희선과제제출\claude\mulcam\backend
python -m uvicorn main:app --reload
```

> `streamlit`, `uvicorn`을 그냥 치면 "내부 또는 외부 명령이 아닙니다" 오류가 난다.
> conda `ml_env`의 `Scripts` 폴더가 PATH에 없기 때문. **반드시 `python -m`을 붙인다.**

전체 검증: `python test_rag.py`

---

## 7. 남은 과제

- 조선일보·머니투데이 봇 차단 우회 (헤더 보강 또는 해당 매체는 RSS 요약 사용)
- 이메일 수신자 관리 방식 미정 (JSON 파일 / DB)
- 발송 주기 자동화 미정
