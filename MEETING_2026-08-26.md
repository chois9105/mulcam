# 회의 브리핑 — 2026-08-26

발표자: 노희선(백엔드) · 2조 (노희선, 문영대, 박윤재, 최승현)

---

## 1. 오늘 결론부터

**백엔드는 실제로 돌아갑니다.** 키워드를 넣으면 실시간으로 뉴스를 모아 요약하고,
검수 점수를 매기고, 승인하면 메일로 보냅니다. MySQL에 저장되고, 정해진 시각에 자동 실행됩니다.

**회의에서 정할 것은 두 가지입니다.**

| # | 정할 것 | 왜 |
|---|---|---|
| 1 | 어느 코드를 최종본으로 쓸 것인가 | 같은 일을 하는 코드가 두 개다 |
| 2 | 화면(프론트엔드)을 누가 맡을 것인가 | 저장소에 회의 때 본 화면 코드가 없다 |

---

## 2. 과제 요구사항 대조

과제 설명서: `프로젝트-...\프로젝트\프로젝트주제_예시.pptx` 슬라이드 2 **뉴스레터 에이전트**

| 요구사항 | 상태 | 어디에 |
|---|---|---|
| 사용자가 관심 키워드 입력 | 완료 | 화면 ① · `POST /api/newsletter/request` |
| 리서치 에이전트 | 완료 | RSS 16곳 + 본문 크롤링 + 검색 |
| 작성 에이전트 | 완료 | 기사별 요약 |
| 검수 에이전트 | 완료 | 별도 모델이 채점 |
| **Conditional Edges** (품질 미달 시 작성으로 순환) | **완료** | `graph_pipeline.py` |
| **Human-in-the-Loop** (`interrupt_before`) | **완료** | `graph_pipeline.py` |
| FastAPI — 초안 조회, 승인/반려 API | 완료 | 엔드포인트 3개 |
| 주기 발송 | 완료 (AWS 아님, APScheduler) | `scheduler.py` |

> **오늘 확인한 것**: 과제가 LangGraph 사용을 명시하고 있었습니다.
> 처음엔 순차 실행으로 만들었는데, 오늘 LangGraph로 다시 구성했습니다.
>
> AWS EventBridge는 과제에 "배포 메리트"로 적혀 있고 필수는 아닙니다.
> 로컬 스케줄러로 같은 일을 합니다.

---

## 3. 지금 동작하는 것 (숫자)

```
뉴스 수집      국내 언론 16곳 · 한 번에 149~181건
기사 본문      149/149 성공 (100%)      RSS 요약 458자 → 본문 900자
요약           기사별 제목 + 한두 문장, 근거 번호 [1][2] 부착
검수           가독성 / 사실 정확도 / 일관성 + 총점
저장           MySQL (서버 껐다 켜도 유지 확인)
발송           지메일 실제 발송 성공
자동 실행      수집 07/12/18시, 생성 08:30
```

### LangGraph 동작 검증 결과

```
1. 그래프 시작       → 인간 승인 노드에서 멈춤 (waiting: True)
2. 멈춘 위치 확인     → 다음 노드 human_approval, 점수 85점 보존
3. 수정 요청          → 작성 단계로 되돌아감 (재작성 1회)
4. 최종 승인          → 발송으로 진행, 종료
```

---

## 4. 오늘 만든 것

| 파일 | 하는 일 |
|---|---|
| `graph_pipeline.py` | **LangGraph** — 순환 구조 + 승인 대기 중단 |
| `static/index.html` | **화면** — AgentLetter Compact |
| `store.py` | MySQL / 메모리 자동 전환 |
| `scheduler.py` | 정해진 시각 자동 실행 |
| `mailer.py` | 메일 발송 + 설정 진단 |

### 화면을 직접 만든 이유

저장소에 **회의 때 본 화면(AgentLetter Compact) 코드가 없습니다.**
`frontend/streamlit_app.py`는 **AgentLetter Pro**라는 다른(예전) 화면입니다.

시연이 안 되면 곤란해서 이미지를 보고 새로 만들었습니다.
백엔드가 화면까지 함께 제공하므로 **서버 하나만 켜면 됩니다.**

```
http://127.0.0.1:8001/
```

---

## 5. 정할 것 ① — 코드가 두 갈래입니다

| | 문영대 `newsletter_m1.py` | 노희선 `backend/` |
|---|---|---|
| LangGraph | 있음 | 있음 (오늘 추가) |
| 저장 | SQLite | **MySQL** (회의 결정사항) |
| 뉴스 출처 | 구글뉴스 + 네이버 | **국내 언론 16곳** |
| 기사 본문 | RSS 요약만 (60자) | **크롤링 900자** |
| 실행 | 명령줄 단독 | **화면이 부르는 서버** |
| 자동 발송 | 있음 | 있음 |

### 제안

`backend/`를 최종본으로 하고, 문영대 님 코드에서 좋은 부분을 가져오기.

**근거**
- 회의에서 MySQL로 정했는데 `newsletter_m1.py`는 SQLite입니다.
- 구글뉴스는 링크가 `news.google.com/rss/articles/...` 리다이렉트라
  **기사 상세페이지로 갈 수 없습니다.** 회의에서 짚었던 문제입니다.
- 네이버 RSS는 서비스가 종료돼 도메인 자체가 없습니다.
- 화면과 붙이려면 서버 형태여야 합니다.

> 문영대 님 검수 채점 기준(사실성35/출처25/구성20/독자20)은 이미 차용했습니다.
> 이 부분은 공유드립니다.

---

## 6. 정할 것 ② — 화면을 누가

| 선택 | 내용 |
|---|---|
| A | 박윤재 님이 화면 코드를 올리고, 제 엔드포인트 3개에 연결 |
| B | 제가 만든 화면(`backend/static/index.html`)을 그대로 씀 |
| C | 제 화면을 바탕으로 박윤재 님이 다듬음 |

**연결할 주소는 세 개뿐입니다.**

```
POST /api/newsletter/request     { request_text }              ① 뉴스레터 요청
POST /api/drafts/{id}/revise     { direction }                 ② 수정 요청
POST /api/drafts/{id}/approve    { frequency }                 ③ 최종 승인
```

응답 하나에 화면이 필요한 게 다 들어 있습니다.

```
title  summary  score  status  frequency_label  created_at
article_html    본문 (근거 링크 포함)
audit_report    가독성 / 사실 정확도 / 일관성 / 검수 의견
sources         참고 기사 + 원문 링크
```

---

## 7. 시연 순서 (5분)

```
1. 서버 켜기          python -m uvicorn main:app --port 8001
2. 화면 열기          http://127.0.0.1:8001/
3. ① 에 키워드 입력   "AI 반도체 관련 뉴스를 정리해 주세요"
                     -> 10~20초 뒤 실제 오늘 뉴스가 요약되어 나옴
4. 카드 펼치기        검수 점수와 근거 기사 확인
                     -> 기사 제목을 누르면 원문으로 이동 (상세페이지 문제 해결)
5. ② 수정 요청        "더 쉽게 설명해 주세요"
                     -> 다시 쓰고 재검수
6. ③ 최종 승인 + 주기  -> 메일 발송
```

**주의**: 메일은 지금 안전장치(`MAIL_DRY_RUN=true`)로 막아뒀습니다.
실제로 보낼 때만 `false`로 바꿉니다. 받는 사람은 팀원 3명으로 설정돼 있습니다.

---

## 8. 남은 일

| 순서 | 할 일 | 담당 |
|---|---|---|
| 1 | 코드 두 갈래 정리 | 팀 |
| 2 | 화면 담당 확정 · 연결 | 팀 |
| 3 | 예외 처리 · 응답 속도 점검 | 팀 |
| 4 | 발표 자료 | 팀 |

과정 안내서상 마지막 날 항목: **"할루시네이션 방지 가드레일 최종 점검"**
→ 이미 확인해 뒀습니다. 기사에 없는 것을 물으면 *"수집된 기사에는 없습니다"* 라고 답합니다.

---

## 9. 발표 때 강조할 만한 것

**① RSS는 본문을 주지 않는다는 것을 직접 확인하고 해결**

| 매체 | RSS가 주는 길이 |
|---|---|
| 연합뉴스 | 63자 |
| 한겨레 | 464자 |
| ZDNet | 1,233자 |

→ 링크를 따라가 본문을 긁어와 **평균 900자** 확보 (성공률 99%)

**② 검색이 망가진 것을 발견하고 고침**

"AI 반도체"를 검색했는데 폭염주의보·의료폐기물 기사가 1위였습니다.
본문 900자를 통째로 임베딩해서 주제가 희석된 탓이었습니다.
세 가지 방식을 실제로 비교해 **제목+앞 200자만 임베딩**으로 바꿨습니다.

→ 과정 안내서의 **"고급 검색 전략(시맨틱 검색, Re-ranking)"** 항목과 맞닿습니다.

**③ 검수가 실제로 감점한다**

하드코딩된 95점이 아닙니다. 근거 번호가 빠지면 실제로 점수가 내려갑니다.
쉽게 풀어 쓰라는 요청에 기사가 통째로 빠지던 문제도 발견해서 고쳤습니다.

> 문제를 찾아 고친 과정은 `TROUBLESHOOTING.md`에 기록해 뒀습니다.
> 코드리뷰 평가 때 근거로 쓸 수 있습니다.

---

## 10. 관련 문서

| 문서 | 내용 |
|---|---|
| `API_SPEC.md` | 엔드포인트 3개 명세 (프론트 담당자에게 전달) |
| `ARCHITECTURE.md` | 전체 구조 · DB 설계 |
| `WBS.md` | 작업 분해 · 일정 · 위험 요소 |
| `TROUBLESHOOTING.md` | 발견해서 고친 문제들 |
| `UI_MAPPING.md` | 화면 요소별 연결표 |
