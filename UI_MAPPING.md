# UI 화면 ↔ 백엔드 연결표

기준 화면: `수정된 화면.docx` — **AgentLetter Compact**
작성일: 2026-08-25 · 백엔드 담당: 노희선

화면 상단 흐름 표시: `Research → Writer → Reviewer → ⏸ Human Approval → Send`
화면 하단 안내: *"현재 리서치/작성/검수 내용과 발송은 데모 로직입니다. 실제 외부 뉴스 검색·LLM·AWS SES 연동은 별도 연결이 필요합니다."*

즉 **화면은 껍데기까지 완성**돼 있고, 그 안을 채우는 것이 백엔드 몫이다.

---

## 한눈에 보기

| 화면 영역 | 백엔드 엔드포인트 | 상태 |
|---|---|---|
| ① 뉴스레터 요청 | `POST /api/newsletter/request` | **신규 필요** |
| ① 주기 선택 | 위 요청에 포함 → `schedules` 저장 | **신규 필요** |
| ② 수정/승인 대상 목록 | `GET /api/drafts?status=pending` | 있음 (`/rag/drafts`) |
| ② 수정 요청 | `POST /api/drafts/{id}/revise` | **신규 필요** |
| ② 최종 승인 | `POST /api/drafts/{id}/approve` | **신규 필요** |
| ③ 뉴스 헤드 목록 | `GET /api/drafts` | 있음 |
| ③ 카드(제목·요약·점수·시각) | 위 응답 필드 그대로 | 있음 |
| ③ 상세 / 검수 결과 | `GET /api/drafts/{id}` | 있음 |
| (숨은 단계) 발송 | `POST /api/drafts/{id}/dispatch` | **신규 필요** |

---

## ① 뉴스레터 요청

```
[ 원하는 뉴스레터 내용을 입력하세요.                        ]
[ 예: 생성형 AI와 LangGraph의 이번 주 주요 뉴스를           ]
[     실무자 관점에서 5개 정도 정리해 주세요.               ]

주기 [ 매일 ▾ ]        [ 🚀 뉴스레터 요청 ]
```

### 여기서 가장 중요한 발견

입력이 **키워드가 아니라 문장**이다. 지금 백엔드는 `topic="AI 반도체"` 같은 짧은 주제만 받는다.
문장에서 아래를 뽑아내는 **요청 분석 단계**가 새로 필요하다.

| 문장에서 뽑아야 할 것 | 예시 문장에서 |
|---|---|
| 검색 키워드 | 생성형 AI, LangGraph |
| 기간 | 이번 주 |
| 독자 | 실무자 |
| 항목 수 | 5개 |
| 출력 형태 | 정리해 주세요 → 표준 뉴스레터(b) |

> 팀원 moonlight 가 만든 `newsletter.py` 의 `analyze_input()` 이 정확히 이 일을 한다.
> 새로 만들지 말고 **가져다 쓰면 된다.**

### 요청 / 응답

```
POST /api/newsletter/request
{
  "request_text": "생성형 AI와 LangGraph의 이번 주 주요 뉴스를 실무자 관점에서 5개 정도 정리해 주세요.",
  "frequency": "daily"
}
```

```
{
  "id": "draft_20260825_165230",
  "title": "LangGraph 맞춤형 뉴스 브리핑",
  "summary": "사용자 요청 '...'을 반영하여 리서치-작성-검수 에이전트가 구성한 초안입니다.",
  "score": 93,
  "status": "pending",
  "created_at": "2026.08.25 16:52",
  "parsed": { "keywords": ["생성형 AI", "LangGraph"], "audience": "실무자", "count": 5 }
}
```

### 내부에서 일어나는 일

```
요청 문장
  → 1. 요청 분석      (신규 - moonlight 코드 재사용)
  → 2. 기사 검색      (완료 - rag_engine)
  → 3. 뉴스레터 작성  (완료 - 템플릿)
  → 4. 한국어 다듬기  (신규 - polisher.py)
  → 5. 검수 채점      (완료 - reviewer)
  → 6. DB 저장        (status = pending)
```

**주기(매일/주간/…)** 는 `schedules` 테이블에 저장한다. 다음 발송 시각에 같은 요청을 자동으로 다시 돌린다.

---

## ② 마음에 안 들 경우 — 수정 요청

```
수정/승인 대상 뉴스  [ LangGraph 맞춤형 뉴스 브리핑 ▾ ]

왜 마음에 안 드나요?              어떻게 바꿔 주세요?
[ 내용이 너무 기술적이고        ] [ 쉽게 줄이고 핵심 뉴스   ]
[ 핵심 뉴스가 잘 보이지 않습니다 ] [ 5개를 먼저 보여주세요.  ]

[ ↺ 수정 요청 ]                  [ ✅ 최종 승인 ]
```

### 대상 목록 채우기

```
GET /api/drafts?status=pending
→ { "drafts": [ { "id": "...", "title": "LangGraph 맞춤형 뉴스 브리핑" } ] }
```

### 수정 요청 — 입력칸이 두 개인 점에 주의

지금 백엔드의 수정 기능은 피드백을 **한 덩어리**로 받는다.
화면은 **"왜"** 와 **"어떻게"** 를 나눠 받으므로, 그대로 나눠서 받는 것이 좋다.
문제와 지시를 구분해서 주면 AI가 더 정확하게 고친다.

```
POST /api/drafts/{id}/revise
{
  "reason":    "내용이 너무 기술적이고 핵심 뉴스가 잘 보이지 않습니다",
  "direction": "쉽게 줄이고 핵심 뉴스 5개를 먼저 보여주세요"
}
```

내부 동작: 기존 초안 + 두 문장을 함께 넣어 다시 작성 → 다듬기 → 재검수 → `revision_count` 1 증가

응답은 새 점수가 매겨진 초안. 화면은 카드만 갱신하면 된다.

### 최종 승인

```
POST /api/drafts/{id}/approve
→ status: pending → approved, 이어서 발송
```

---

## ③ 선택했던 뉴스들의 헤드 내용

```
출력할 뉴스 헤드 선택
[ LangGraph 맞춤형 뉴스 브리핑 ✕ ]                      ▾

┌──────────────────────────────────────────────┐
│ LangGraph 맞춤형 뉴스 브리핑                  │
│ 사용자 요청 '...'을 반영하여 리서치-작성-검수  │
│ 에이전트가 구성한 맞춤형 뉴스레터 초안입니다.  │
│ 검수 93점 · 승인 대기 · 2026.08.25 16:52     │
└──────────────────────────────────────────────┘

> 선택 뉴스의 상세 내용 / 검수 결과
```

카드에 필요한 값은 **이미 백엔드가 다 주고 있다.**

| 화면 표시 | 백엔드 필드 |
|---|---|
| 제목 | `title` |
| 설명 | `summary` |
| 검수 93점 | `score` |
| 승인 대기 | `status` = `pending` |
| 2026.08.25 16:52 | `created_at` |

### 상세 펼침

```
GET /api/drafts/{id}
```

| 화면 | 백엔드 필드 |
|---|---|
| 뉴스레터 본문 | `article_html` (HTML, 근거 링크 포함) |
| 검수 결과 | `audit_report` (가독성 / 사실정확도 / 일관성 / 의견) |
| 참고 기사 | `sources` (제목 + 원문 링크) |

> `sources` 의 링크로 **기사 상세페이지로 넘어갈 수 있다.**
> 회의에서 짚으신 "제목만 나오면 상세페이지로 못 간다" 문제는 여기서 해결된다.

---

## 새로 만들어야 할 것 (우선순위)

| # | 작업 | 이유 | 참고 |
|---|---|---|---|
| **1** | **요청 문장 분석** | 화면이 키워드가 아닌 문장을 받는다 | `newsletter.py` 의 `analyze_input()` 재사용 |
| **2** | `POST /api/newsletter/request` | ① 버튼이 부를 창구 | 기존 `/rag/draft` 를 감싸면 된다 |
| **3** | `POST /api/drafts/{id}/revise` | ② 수정 요청. **입력 2개** | 재작성 → 재검수 |
| **4** | `POST /api/drafts/{id}/approve` | ② 최종 승인 | 상태 변경 + 발송 |
| **5** | 초안을 DB에 저장 | 지금은 메모리라 서버 끄면 사라짐 | `drafts` 테이블 |
| **6** | 주기 저장 + 자동 실행 | ① 주기 선택 | `schedules` + APScheduler |
| **7** | 한국어 다듬기 | 회의 결정 | `polisher.py` |

---

## 팀에 확인할 것

| # | 내용 |
|---|---|
| 1 | 화면 안내문에 **AWS SES** 라고 적혀 있다. 지금 백엔드는 Gmail SMTP 로 되어 있는데, SES 로 갈지 정해야 한다. |
| 2 | `frontend/main.py` 에 있던 예전 화면(`/api/keywords`, 배치 승인 등)은 이제 안 쓰는 것인지 확인 필요. 이 화면에는 키워드 등록 칸이 없다. |
| 3 | ③번의 뉴스 헤드 **다중 선택**은 여러 초안을 한 번에 보는 용도인지, 한 통에 합쳐 보내는 용도인지 확인 필요. |
| 4 | moonlight 님께 `analyze_input()` 재사용 공유. |
