"""
기본 템플릿 3종 (a / b / c) 와 기본 검수 지침

회의 결정:
    "사용자 추천을 받은 파이썬 파일 안에 기본 템플릿을 저장해둔다. 3개 정도"
    "이 템플릿의 형태를 MySQL 에 저장해둠"

그래서 기본값은 이 파일에 두고, 서버가 처음 뜰 때 MySQL 에 넣는다(seed).
사용자가 화면에서 고치면 MySQL 값이 바뀌고, 그때부터는 MySQL 이 우선이다.
이 파일은 "공장 초기화" 기준값 역할을 한다.
"""

# 모든 템플릿에 공통으로 붙는 규칙
COMMON_RULES = """규칙:
- 아래 [기사]에 있는 내용만 쓴다. 없는 사실·수치는 지어내지 않는다.
- 각 항목 끝에 근거 기사 번호를 [1], [2] 형태로 붙인다.
- 마크다운, 한국어로 작성한다."""


DEFAULT_TEMPLATES = [
    {
        "code": "a",
        "name": "짧은 브리핑",
        "description": "출퇴근길에 훑는 200자 이내 3줄 요약",
        "style": "brief",
        "article_count": 5,
        "prompt_body": f"""당신은 뉴스 큐레이터입니다. '{{topic}}' 주제로 아주 짧은 브리핑을 만듭니다.
형식:
- 오늘의 한 줄 (헤드라인 1문장)
- 핵심 3가지, 각 한 문장 [n]
전체 200자 이내로 압축한다.
{COMMON_RULES}""",
    },
    {
        "code": "b",
        "name": "표준 뉴스레터",
        "description": "이슈 3~5개를 소제목과 함께 정리한 기본형",
        "style": "newsletter",
        "article_count": 8,
        "prompt_body": f"""당신은 뉴스레터 편집자입니다. '{{topic}}' 주제의 뉴스레터를 작성합니다.
형식:
- # 제목 (한 줄 헤드라인)
- 핵심 이슈 3~5개. 각 항목은 **소제목** + 2~3문장 설명 [n]
- 마지막에 '오늘의 한 줄' 정리
{COMMON_RULES}""",
    },
    {
        "code": "c",
        "name": "심층 분석",
        "description": "사실관계·의미·흐름까지 짚는 분석형",
        "style": "deep",
        "article_count": 12,
        "prompt_body": f"""당신은 뉴스 분석가입니다. '{{topic}}' 주제를 깊이 있게 정리합니다.
형식:
- # 제목
- ## 무슨 일이 있었나 : 사실관계 정리 [n]
- ## 왜 중요한가 : 기사에 드러난 파급효과·이해관계 [n]
- ## 함께 볼 흐름 : 서로 연결되는 기사들을 묶어 설명 [n]
- ## 참고 기사 : 제목과 링크 목록
해석을 덧붙이되, 근거는 반드시 기사 안에서 찾는다.
{COMMON_RULES}""",
    },
]


# 회의: "지침을 줘서 참고해서 검수할 때 이럴 때 이렇게 해라"
DEFAULT_GUIDELINE = {
    "name": "기본 검수 지침",
    "pass_score": 80,
    "weights": {"사실성": 35, "출처": 25, "구성": 20, "독자적합성": 20},
    "content": """당신은 엄격한 뉴스레터 편집장이다. 아래 기준으로 채점한다.

[배점]
- 사실성 35점 : 근거 기사에 없는 주장·수치가 있으면 감점
- 출처 25점   : 문장마다 근거 번호 [n] 이 붙어 있는가
- 구성 20점   : 제목-본문-정리 흐름이 매끄러운가
- 독자적합성 20점 : 일반 독자가 읽기에 적절한 난이도인가

[이럴 때 이렇게 한다]
- 근거 없는 단정("~할 것이다", "확실하다")이 있으면 사실성에서 10점 이상 깎는다.
- 근거 번호가 하나도 없으면 출처 점수를 0점 처리한다.
- 같은 내용이 여러 항목에 반복되면 구성에서 감점한다.
- 전문용어를 풀어쓰지 않았으면 독자적합성에서 감점한다.
- 기사 제목만 나열하고 설명이 없으면 구성·독자적합성 모두 감점한다.
- 오탈자나 어색한 번역투 문장이 있으면 지적하고 고칠 문장을 제시한다.

[통과 기준]
총점 80점 이상만 통과다. 통과해도 개선 의견을 1개 이상 반드시 적는다.""",
}


def get_template_by_code(code: str) -> dict | None:
    """코드(a/b/c)로 기본 템플릿을 찾는다."""
    for t in DEFAULT_TEMPLATES:
        if t["code"] == code:
            return t
    return None


# 기존 rag_engine 의 style 이름과 템플릿 코드를 잇는다
STYLE_TO_CODE = {"brief": "a", "newsletter": "b", "deep": "c"}
CODE_TO_STYLE = {v: k for k, v in STYLE_TO_CODE.items()}
